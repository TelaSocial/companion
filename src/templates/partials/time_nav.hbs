<nav
    id="time-nav"
    class="navbar navbar-default navbar-fixed-bottom"
    >
    <ul
        class="nav navbar-nav"
        role="navigation"
        >
        {{#each schedule.days}}
            <li>{{this.shortLabel}}</li>
            {{> timenav_day_times dayIndex=@index}}
        {{/each}}
    </ul>
</nav>
